use crate::sample::Sample;

// Si no tenés la carpeta utils o el archivo, esto ya no es necesario
// Si necesitás el struct Recording, asegurate de definirlo aquí mismo
pub struct Recording {
    pub sample: Sample,
}
